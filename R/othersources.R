# Calculate total RF-EMF dose (for brain and body) from other exposure sources

# =============================================================================
#' Calculate Dose from other exposure sources
#'
#' @param dur_tablet Duration of other exposure sources in seconds
#' @param params Parameter list
#' @returns List with brain dose and body dose in mJ/kg/day
#' @export
get_other_dose <- function() {
  return(NA)
}

# =============================================================================
#' Calculate exposure sources Aggregated Power
get_other_pwr <- function() {
  return(NA)
}

# =============================================================================
#' Calculate exposure sources Aggregated SAR
get_other_sar <- function() {
  return(NA)
}
